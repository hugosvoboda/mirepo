<h2><%= link_to "Holiday Reminder", root_path %></h2>

<p>Usuario Actual: <strong><%= current_user.first_name %></strong></p>
<h2>Lista de Feriados</h2>

<p id="notice"><%= notice %></p>

<table>
  <tr>
    <th>Descripci&oacute;n</th>
    <th>Fecha</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @holidays.each do |holiday| %>
  <tr>
    <td><%= holiday.description %></td>
    <td><%= holiday.date %></td>
    <td><%= link_to 'Ver', holiday %></td>
    <td><%= link_to 'Modificar', edit_holiday_path(holiday) %></td>
    <td><%= link_to 'Eliminar', holiday, method: :delete, data: { confirm: '¿Ud. está seguro que desea Eliminar el Feriado?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nuevo Feriado', new_holiday_path %>&nbsp;|&nbsp;
<%= link_to "Clientes", costumers_path, :method => :get %>&nbsp;|&nbsp;
<%= link_to "Salir", destroy_user_session_path, :method => :delete %>